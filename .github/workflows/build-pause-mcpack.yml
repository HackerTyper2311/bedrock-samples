name: Build Polished Deepslate UI MCPack

on:
  push:
    branches:
      - demo-pause-release
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: custom_pause_pack

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate JSON
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          for p in [Path('manifest.json'), Path('ui/_ui_defs.json'), Path('ui/pause_screen.json')]:
              json.loads(p.read_text(encoding='utf-8'))
              print('OK:', p)
          PY

      - name: Build MCPack
        run: |
          cd "$GITHUB_WORKSPACE"
          rm -f Polished_Deepslate_UI.mcpack
          cd custom_pause_pack
          zip -r "$GITHUB_WORKSPACE/Polished_Deepslate_UI.mcpack" .

      - name: Verify MCPack
        run: |
          test -f "$GITHUB_WORKSPACE/Polished_Deepslate_UI.mcpack"
          unzip -t "$GITHUB_WORKSPACE/Polished_Deepslate_UI.mcpack"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: Polished-Deepslate-UI-mcpack
          path: ${{ github.workspace }}/Polished_Deepslate_UI.mcpack
          if-no-files-found: error

      - name: Create demo release
        if: github.ref_name == 'demo-pause-release'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="polished-deepslate-${GITHUB_RUN_NUMBER}"
          gh release create "$TAG" "$GITHUB_WORKSPACE/Polished_Deepslate_UI.mcpack" \
            --repo "$GITHUB_REPOSITORY" \
            --title "Polished Deepslate UI" \
            --notes "Demo release of Polished Deepslate UI for Minecraft Bedrock. The pause button background and hover texture are replaced while Minecraft renders the button text itself." \
            --prerelease
